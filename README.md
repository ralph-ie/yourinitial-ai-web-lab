# AI Web Lab

This project demonstrates a basic AI-powered web application using:
- TensorFlow.js for client-side image classification
- Node.js with the OpenAI API for text-based AI interaction

The OpenAI API key is stored locally using environment variables and excluded from version control.
